# Database Configuration
DB_USER="backe"
DB_PASSWORD=""
DB_HOST="217.154.223.224"
DB_PORT="3306"
DB_NAME="ProductionDB"

# FastAPI / JWT Configuration
SECRET_KEY="" # CHANGE THIS!
ACCESS_TOKEN_EXPIRE_MINUTES="20"
REFRESH_TOKEN_EXPIRE_MINUTES="360000"
SECURE_COOKIE=true


# Google OAuth Configuration
GOOGLE_CLIENT_ID="504142517618-k0a579vvbv2mohnodov2ngtbhimo8gnf.apps.googleusercontent.com" # Replace with your actual client ID
GOOGLE_CLIENT_SECRET="" # Replace with your actual client secret
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"
FRONTEND_BASE_URL="http://localhost:3000/oauth/callback"


# Google GenAI Configuration
GOOGLE_APPLICATION_CREDENTIALS="dev-poet-461212-d9-35a36f7ab681.json"
GOOGLE_API_KEY=
GOOGLE_PROJECT_ID=
GOOGLE_GENAI_USE_VERTEXAI=FALSE


GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI="http://localhost:3000/api/auth/github/callback"


DISCORD_CLIENT_ID=""
DISCORD_CLIENT_SECRET=""
DISCORD_REDIRECT_URI="http://localhost:3000/api/auth/discord/callback"

UNSPLASH_ACCESS_KEY=""
UNSPLASH_SECRET_KEY=""


# ChromaDB Configuration
CHROMA_HOST=chromadb
CHROMA_PORT=8000
CHROMA_CLIENT_TYPE=http
CHROMA_COLLECTION_NAME=nexora_content
EMBEDDING_MODEL=all-MiniLM-L6-v2
CHROMA_CLIENT_TYPE="http" # persistent nicht mehr möglich!

# Minio Credentials
MINIO_ROOT_USER=ROOTNAME
MINIO_ROOT_PASSWORD=CHANGEME123

# App specific
WORKERS=1

# SSH Restart Configuration (optional - for remote Docker Compose restart)
# If enabled, the admin restart endpoint will execute 'docker compose restart' via SSH
# instead of sending SIGTERM to the local process
SSH_RESTART_ENABLED=false
SSH_RESTART_HOST=""                           # e.g., "myserver.example.com"
SSH_RESTART_USER=""                           # e.g., "deploy"
SSH_RESTART_KEY_PATH=""                       # e.g., "/path/to/ssh/key" (optional, uses default SSH config if not set)
SSH_RESTART_COMPOSE_PATH=""                   # e.g., "/opt/nexora/backend"
SSH_RESTART_PROJECT_NAME="nexora"             # Docker Compose project name

# Admin Quick Links (configurable URLs for admin panel tools)
QUICK_LINK_PGADMIN_PROD="https://db.nexora-ai.de"
QUICK_LINK_PGADMIN_DEV="https://db.dev.nexora-ai.de"
QUICK_LINK_PGADMIN_LOCAL="http://localhost:5050"

QUICK_LINK_MINIO_PROD="https://minidb.nexora-ai.de/login"
QUICK_LINK_MINIO_DEV="https://minidb.dev.nexora-ai.de/login"
QUICK_LINK_MINIO_LOCAL="http://localhost:9001"

QUICK_LINK_WEBSITE_PROD="https://www.nexora-ai.de"
QUICK_LINK_WEBSITE_DEV="https://dev.nexora-ai.de"
QUICK_LINK_WEBSITE_LOCAL="http://localhost:3000"

QUICK_LINK_CLOUDPANEL="https://cp.nexora-ai.de"
QUICK_LINK_GITHUB="https://github.com/Nexora-AI-de/Nexora"
QUICK_LINK_ANALYTICS="https://analytics.nexora-ai.de"

  
  

# Postgres configuration
POSTGRES_DB=nexora
POSTGRES_USER=nexora
POSTGRES_PASSWORD=your-secure-password

# pgAdmin defaults (optional)
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=adminpass

# MinIO defaults (example)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadminpass

# Other optional envs used by the app
# DB_NAME/DB_USER/DB_PASSWORD will be read from POSTGRES_* variables if set in the compose
# You can also override them directly:
# DB_NAME=${POSTGRES_DB}
# DB_USER=${POSTGRES_USER}
# DB_PASSWORD=${POSTGRES_PASSWORD}
