# Step 4: Intelligent Traffic Routing

We added a **Global HTTPS Load Balancer** with intelligent URL-based routing: API calls hit the backend service, while static assets are served directly from Cloud Storage via a **backend bucket**, optimizing resource utilization and reducing latency.
