# Step 5: Global Edge Caching

**Cloud CDN** sits in front of both the frontend Cloud Run service and the static asset bucket, caching content at Google's global edge network with **150+ PoPs worldwide**, dramatically reducing latency for users across all continents.
