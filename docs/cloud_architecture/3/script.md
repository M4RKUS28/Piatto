# Step 3: Persistent Data Layer

For data persistence, we integrated **Cloud SQL with MySQL** via **VPC connector** for structured data and **Cloud Storage buckets** for media files and images, ensuring scalable, managed storage with built-in redundancy.
